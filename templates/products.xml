<Root>
    <Products>
        {% for product in products %}
        <Product>
            <Vendor>{{ product.vendor }}</Vendor>
            <Language>{{ product.language }}</Language>
            <ProductName>{{ product.name }}</ProductName>
			<ProductCode>{{ product.code }}</ProductCode>
			<Category>{{ product.category }}</Category>
			<Brand>{{ product.brand }}</Brand>
			<ModelType>{{ product.model_type }}</ModelType>
            <Condition>{{ product.condition }}</Condition>
			<Origin>{{ product.origin }}</Origin>
			<Delivery_status>{{ product.delivery_status }}</Delivery_status>
			<Guarantee>{{ product.guarantee }}</Guarantee>
            <Currency>{{ product.currency }}</Currency>
			<Price>{{ product.price }}</Price>
			<Price_Description>{{ product.price_description }}</Price_Description>
			<Phone>{{ product.phone }}</Phone>
			<Whatsapp>{{ product.whatsapp }}</Whatsapp>
			<Description><![CDATA[{{ product.description|safe }}]]></Description>
            <Source>{{ product.url }}</Source>
            <Images>{% for img in product.images.all %}
                <Image{{ forloop.counter }}>{{ img.image }}</Image{{ forloop.counter }}>{% endfor %}
            </Images>
        </Product>{% endfor %}
    </Products>
</Root>